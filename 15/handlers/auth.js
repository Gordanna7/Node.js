const bcrypt = require("bcryptjs");
const user = require("../pkg/user");

const signUp = async (req, res) => {
  try {
  } catch (err) {
    console.log(err);
    return res.status(500).send("intertnal srv err");
  }
};
